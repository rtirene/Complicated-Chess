<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobileapp.polimi.maprojectg4">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_TASKS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".view.LoadingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.GameActivity" />
        <activity android:name=".controller.MainActivity" />
        <activity android:name=".controller.PlayActivity" />
        <activity android:name=".controller.ReplayActivity" />
        <activity android:name=".controller.RulesActivity" />
        <activity android:name=".controller.OnlineGameActivity" />
        <activity android:name=".controller.LoadActivity" />
        <activity android:name=".controller.SeeMatchActivity" />

        <service android:name=".controller.SoundService" />

        <activity android:name=".controller.WaitOnlineGameActivity"></activity>
    </application>

</manifest>